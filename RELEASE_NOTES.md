# Release Notes

## 1.0.0-rc16

### 搜索结果定位

- 搜索结果悬浮信息中的“分类”改为符号的原始静态分类，不再显示“搜索结果”。
- 在分类下新增“信息”，显示符号在原始分类中的行号和该行位置。
- 构建阶段生成 `YSSymbolOriginRecord` 索引，为每个内置符号记录原始分类、分组、行号和列号。
- 右键菜单新增“跳到所在位置”，可清空搜索、展开所需分类并滚动到原始符号位置。
- 只存在于内部“补充符号”的项目会定位到“全部符号”中的对应位置，同时保留原始分类说明。

### 搜索与侧边栏

- 搜索历史保存到当前用户注册表；搜索框使用 `↑`、`↓` 回显历史和当前草稿。
- 左侧分类增加淡色鼠标悬停背景。
- “其他字符”更名为“其他符号⯈/⯆”，使用与普通分类一致的字体、字号和字重。
- 点击“其他符号”后，选择状态停留在该标题上，不再跳回“常用符号”。

### 界面参数

- 主窗口尺寸改为 `786 × 650`。
- 左侧分类宽度改为 `162`，分类项高度改为 `28`。

### 数据链路与文档

- 保持 `catalog.txt → catalog.generated.json → symbol_data.c → yesymbol.exe` 构建链路。
- JSON只在构建期由Python转换为静态C数组；运行时不读取JSON。
- 静态回归新增TXT、JSON、C数量、符号来源索引和分类元数据一致性检查。
- README重写，补充项目介绍、开发目的、安装使用、编译运行、TXT生成JSON以及开发维护说明。

## 1.0.0-rc15

### 分类层级

- 将“数字序号”和“字母序号”合并为“序号字母”。
- 主分类固定为常用、特殊、标点、序号字母、数学、语言和五个 Emoji 分类。
- 新增可折叠的“其他字符”层级，包含日文、韩文、东亚、大篆、小篆、俄文、古埃及文字和象形文字。
- “全部符号”固定在倒数第二项，“自定义”固定为最后一项。
- “补充符号”继续保留在静态目录和搜索/全部符号中，但不再单独占用侧边栏入口。
- 大篆和小篆作为字形类别预留；Unicode 没有为其单独编码，界面会显示字体资源说明而不是伪造字符。

### 自动插入

- 自动插入改为先恢复此前目标窗口和焦点控件，再发送 `Ctrl+V`。
- 删除固定 `Sleep(25)` 等待，使用前台窗口确认和短暂线程让步。
- 剪贴板成功后先执行自动插入，再更新最近使用和常用频次。
- 最近使用、常用和自定义记录改为350毫秒延迟合并写入注册表，避免同步写入阻塞点击。

### 启动与浏览性能

- 构建阶段生成 UTF-16 FNV-1a 开放寻址哈希表，符号文本查找由线性扫描改为接近常数时间。
- 托盘图标和两个悬浮提示窗口延后到主界面首次创建完成后初始化。
- 搜索输入增加90毫秒节流；连续输入不再为每个按键立即重建结果。
- 480项以上的静态分类启用虚拟布局，只保存分组和行范围，只绘制当前可见行。
- 大型搜索结果使用扁平虚拟绘制，避免为每个结果建立 `YSLayoutItem`。
- 主符号区复用兼容位图和内存 DC，减少滚动、悬停时反复创建和销毁 GDI 对象。
- 搜索最近使用、常用和自定义时复用哈希去重标记，减少宽泛搜索中的重复字符串比较。

### 数据与兼容性

- 符号总数保持16,679项，未因分类重组丢失数据。
- 保留 `/MANIFEST:NO`、固定窗口、单实例、托盘、CLDR中文名称和Windows权限写入修复。
- 当前Linux环境不能执行MSVC和Win32交互测试，最终启动耗时与自动插入体感需在Windows上验收。

## 1.0.0-rc13

### Windows 数据生成稳定性

- 修复连续执行 `regenerate-data.cmd` 与 `build.bat run` 时，第二次写入 `src\symbol_data.c` 可能触发 `PermissionError`。
- 生成器会先比较完整字节；内容未变化时直接跳过写入，不再触碰被编辑器或安全软件短暂占用的文件。
- 内容变化时继续采用解除只读、临时文件、原子替换和重试机制。

### CLDR 中文名称

- 固定使用 Unicode CLDR 提交 `c9a5503bf238114a1993377b87841fb76031371d` 的 `common/annotations/zh.xml`。
- 优先读取 `type="tts"` 中文短名称，并按 CLDR 规则忽略 `U+FE0F` 后匹配。
- 新增本地缓存和离线回退；发布后的 EXE 不访问网络。
- 新增 `build.bat cldr`，用于刷新官方数据、更新低质量中文名称并重新生成目录。

### 目录完整性与整理

- 唯一符号或 Unicode 序列增加到 16,679 项。
- 补齐带圈数字 21–50，结尾包含 `㊿`。
- 补齐259个国家和地区旗帜，以及英格兰、苏格兰、威尔士3个地区旗帜；区域指示字母 A–Z 仍保留。
- 麻将牌、多米诺骨牌、扑克牌和国际象棋扩展符号改为完整 Unicode 顺序。
- 新增“象形文字”和“古埃及文字”，并放在“补充符号”之前；古埃及文字共1,110项。
- 将标点、数学、箭头、图形、制表、东亚、牌类和技术符号尽量从“补充符号”迁移到已有分类，补充符号可见项由2,169项降至970项。
- 旗帜记录补齐中文地区名称；即使系统字体把旗帜序列显示成字母，悬浮名称和复制内容仍然正确。

## 1.0.0-rc12

- 一级分类直接由 `catalog.txt` 正文中的 `# 标题` 生成。
- 支持一级分类后直接写符号行；超过12项自动换行。
- 新增“字母序号”并补齐七套 A–Z。
- `build.bat run` 会先生成并审计目录，失败时停止。

## 1.0.0-rc11

### 人工可编辑目录

- 新增 `data-source/catalog.txt`，采用接近 Markdown 的层级语法。
- 分类使用 `#`，分组使用 `##`，符号行使用无歧义的 JSON 数组。
- 文件末尾的 `@symbol` 记录可以直接修改中文名称、英文名称和 Emoji 标记。
- 新增 `tools/catalog_text.py`，支持 `export`、`build`、`check` 和 `dedupe`。
- `regenerate-data.cmd` 改为先从 `catalog.txt` 生成 JSON，再生成静态 C 数据。

### 去重

- 保留全部 13,931 个唯一符号或 Unicode 序列，没有丢失历史补充。
- 清理同一分组中的 6 个重复项目。
- 清理同一分类不同分组间的 146 个重复引用。
- 不同分类之间允许复用同一符号，避免破坏分类查找入口。
- “全部符号”不再在文本源中重复维护，而是自动生成唯一并集。
- 1,691 个原来只存在于“全部符号”的项目移动到“补充符号 → 未分类补充”。

### 界面

- 删除分类说明行，包括“全部内置符号的去重集合；可搜索、复制或右键添加到常用符号。”。
- 右侧符号网格上移，与左侧分类列表顶部对齐。
- 默认固定窗口宽度改为 `770`，高度保持 `650`。

## 1.0.0-rc9

### 图标与品牌

- 增加 YeSymbol 专用多尺寸 Windows 图标。
- 图标统一应用到 EXE、窗口标题栏、任务栏和系统托盘。
- 图标源文件位于 `assets/yesymbol.ico`，README 预览图位于 `assets/yesymbol-512.png`。

### 关于窗口

- 点击窗口左上角图标打开系统菜单，可进入“关于 YeSymbol...”。
- 托盘右键菜单增加“关于 YeSymbol”。
- 关于窗口显示版本、开发日期、开发目的和联系邮箱。
- 联系邮箱 `yuxiang_163com@163.com` 可点击并唤起默认邮件程序。
- 预留网页版链接配置。

## 1.0.0-rc8

- 删除 `build-clean.bat` 和 `autorun.bat`，统一使用 `build.bat`。
- 新增 `build`、`clean`、`all`、`run` 和 `help` 子命令。
- 修复已有 CMake 缓存与 `-A x64` 冲突导致的 generator platform mismatch。
- Emoji 专题分类重新分组。
- Emoji 分类默认优先显示基础/黄色皮肤版本。
- 右键支持肤色变体选择。
- 所有分组统一限制为每行最多 12 个符号。

## 1.0.0-rc5

- 增加系统托盘；关闭窗口时隐藏到托盘。
- 悬浮提示精简。
- 主窗口改为固定尺寸。
- 界面关键尺寸参数集中到 `include/ui_config.h`。
